<UserControl x:Class="Kursach.View.Forms.NewSupplier"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Добавить нового поставщика" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Название компании:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SupplierName, UpdateSourceTrigger=PropertyChanged}" Height="25" Margin="5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Контактное лицо:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ContactPerson, UpdateSourceTrigger=PropertyChanged}" Height="25" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Телефон:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Height="25" Margin="5"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Email:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="25" Margin="5"/>

        <Button Grid.Row="5" Grid.ColumnSpan="2" 
                Content="Сохранить" 
                Command="{Binding SaveSupplierCommand}"
                FontSize="14"
                Height="35"
                Width="200"
                Margin="0,20,0,0"/>
    </Grid>
</UserControl>