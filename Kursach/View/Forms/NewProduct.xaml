<UserControl x:Class="Kursach.View.Forms.NewProduct"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kursach.View.Forms">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>  
            <RowDefinition Height="30"/>  
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/> <!-- New row for IsPremiumBrand -->
            <RowDefinition Height="120"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.ColumnSpan="2"
               Content="{Binding FormTitle}"
               FontSize="20" FontWeight="Bold"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Тип"></Label>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Type, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="2" Grid.Column="0" Content="Бренд"></Label>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="3" Grid.Column="0" Content="Модель"></Label>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="4" Grid.Column="0" Content="Серийный номер"></Label>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="5" Grid.Column="0" Content="Цена"></Label>
        <TextBox Grid.Row="5" Grid.Column="1"
                 Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="6" Grid.Column="0" Content="Кол-во на складе"></Label>
        <TextBox Grid.Row="6" Grid.Column="1"
                 Text="{Binding QuantityInStock, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        <Label Grid.Row="7" Grid.Column="0" Content="Гарантийный срок (мес.)"></Label>
        <TextBox Grid.Row="7" Grid.Column="1"
                 Text="{Binding WarrantyPeriod, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        
        <Label Grid.Row="8" Grid.Column="0" Content="Поставщик"></Label>
        <ComboBox Grid.Row="8" Grid.Column="1"
                  ItemsSource="{Binding Suppliers}"
                  SelectedItem="{Binding SelectedSupplier}"
                  DisplayMemberPath="Name"
                  HorizontalAlignment="Left"
                  Width="200"
                  Height="25"/>
       
       <Label Grid.Row="9" Grid.Column="0" Content="Премиум бренд"></Label>
       <CheckBox Grid.Row="9" Grid.Column="1"
                 IsChecked="{Binding IsPremiumBrand, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 VerticalAlignment="Center"/>

       <Label Grid.Row="10" Grid.Column="0" Content="Изображение"></Label>
       <StackPanel Grid.Row="10" Grid.Column="1" Orientation="Vertical">
           <Button Content="Выбрать изображение"
                   Command="{Binding SelectImageCommand}"
                   HorizontalAlignment="Left"
                   Width="150"
                   Height="30"
                   Margin="0,0,0,10"/>
           <Border BorderBrush="#CCCCCC"
                   BorderThickness="1"
                   Background="#F5F5F5"
                   Width="150"
                   Height="100"
                   HorizontalAlignment="Left">
               <Image Source="{Binding ImagePreviewPath}"
                      Width="150"
                      Height="100"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
           </Border>
       </StackPanel>
       
       <Button Grid.Row="12" Grid.ColumnSpan="2"
               Content="Сохранить"
               Command="{Binding SaveCommand}"
               FontSize="14"
               Height="35"
               Margin="0,10,0,0"/>
    </Grid>
</UserControl>
