<UserControl x:Class="Kursach.View.Forms.NewSeller"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Добавить нового продавца" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Имя продавца:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding SellerName, UpdateSourceTrigger=PropertyChanged}"
                 Height="25" Margin="5"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" 
                Content="Сохранить" 
                Command="{Binding SaveSellerCommand}"
                FontSize="14"
                Height="35"
                Width="200"
                Margin="0,20,0,0"/>
    </Grid>
</UserControl>