<UserControl x:Class="Kursach.View.Forms.NewClient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kursach.View.Forms">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>  
            <RowDefinition Height="30"/>  
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Добавление клиента" FontSize="20" FontWeight="Bold"></Label>
        
        <Label Grid.Row="1" Grid.Column="0" Content="ФИО"></Label>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Номер телефона"></Label>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        
        <Label Grid.Row="3" Grid.Column="0" Content="Email"></Label>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        
        <Label Grid.Row="4" Grid.Column="0" Content="История покупок"></Label>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding PurchaseHistory, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalAlignment="Left"
                 Width="200"
                 Height="25"/>
        
        <Button Grid.Row="5" Grid.ColumnSpan="2" 
                Content="Сохранить" 
                Command="{Binding SaveCommand}"
                FontSize="14"
                Height="35"
                Width="350"
                Margin="0,10,0,0"/>
    </Grid>
</UserControl>
