<UserControl x:Class="Kursach.View.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kursach.View"
      xmlns:viewModel="clr-namespace:Kursach.ViewModel"
      xmlns:converters="clr-namespace:Kursach.Converters"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="900">
    <UserControl.Resources>
        <converters:ImagePathConverter x:Key="ImagePathConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:CurrentPriceConverter x:Key="CurrentPriceConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <viewModel:MainViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Каталог товаров" 
                       FontSize="24" 
                       FontWeight="Bold"/>
            <TextBlock Text="{Binding FilteredProducts.Count, StringFormat=' ({0} товаров)'}" 
                       FontSize="18" 
                       Foreground="Gray"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>
        </StackPanel>
        
        <!-- Панель фильтров -->
        <Border Grid.Row="1" 
                Background="#F5F5F5" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1"
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Row="0" Grid.ColumnSpan="9" 
                           Text="Фильтры" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"/>
                
                <Label Grid.Row="1" Grid.Column="0" 
                       Content="Тип:" 
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1" 
                          ItemsSource="{Binding Types}"
                          SelectedItem="{Binding SelectedType}"
                          Width="150"
                          Height="30"
                          Margin="0,0,15,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <Label Grid.Row="1" Grid.Column="2" 
                       Content="Бренд:" 
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"/>
                <ComboBox Grid.Row="1" Grid.Column="3" 
                          ItemsSource="{Binding Brands}"
                          SelectedItem="{Binding SelectedBrand}"
                          Width="150"
                          Height="30"
                          Margin="0,0,15,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <Label Grid.Row="1" Grid.Column="4" 
                       Content="Цена от:" 
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"/>
                <TextBox Grid.Row="1" Grid.Column="5" 
                         Text="{Binding MinPrice, UpdateSourceTrigger=PropertyChanged}"
                         Width="100"
                         Height="30"
                         Margin="0,0,15,0"/>
                
                <Label Grid.Row="1" Grid.Column="6" 
                       Content="до:" 
                       VerticalAlignment="Center"
                       Margin="0,0,5,0"/>
                <TextBox Grid.Row="1" Grid.Column="7" 
                         Text="{Binding MaxPrice, UpdateSourceTrigger=PropertyChanged}"
                         Width="100"
                         Height="30"
                         Margin="0,0,15,0"/>
                
                <Button Grid.Row="1" Grid.Column="8" 
                        Content="Сбросить" 
                        Command="{Binding ClearFiltersCommand}"
                        Width="100"
                        Height="30"
                        Background="#757575"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#757575"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#616161"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
        
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredProducts}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCCCCC" 
                                BorderThickness="1" 
                                CornerRadius="5" 
                                Background="White"
                                Margin="10"
                                Padding="15"
                                Width="250"
                                Height="400">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="120"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Border Grid.Row="0" 
                                        Background="#F5F5F5" 
                                        BorderBrush="#E0E0E0" 
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Height="120"
                                        Margin="0,0,0,10">
                                    <Image Stretch="Uniform"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           MaxWidth="200"
                                           MaxHeight="120">
                                        <Image.Source>
                                            <Binding Path="ImagePath" Converter="{StaticResource ImagePathConverter}">
                                                <Binding.FallbackValue>
                                                    <BitmapImage UriSource="pack://application:,,,/Media/watch.ico"/>
                                                </Binding.FallbackValue>
                                            </Binding>
                                        </Image.Source>
                                    </Image>
                                </Border>
                                
                                <TextBlock Grid.Row="1" 
                                           Text="{Binding Brand}" 
                                           FontSize="18" 
                                           FontWeight="Bold"
                                           Margin="0,10,0,5"
                                           TextAlignment="Center"/>
                                
                                <StackPanel Grid.Row="2" Margin="0,5">
                                    <TextBlock Text="{Binding Model}" 
                                               FontSize="14" 
                                               Margin="0,2"/>
                                    <TextBlock Text="{Binding Type}" 
                                               FontSize="12" 
                                               Foreground="Gray"
                                               Margin="0,2"/>
                                    <TextBlock Text="{Binding SerialNumber}" 
                                               FontSize="11" 
                                               Foreground="DarkGray"
                                               Margin="0,2"/>
                                    <TextBlock Margin="0,5">
                                        <Run Text="На складе: " FontWeight="Bold"/>
                                        <Run Text="{Binding QuantityInStock}"/>
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Гарантия: " FontWeight="Bold"/>
                                        <Run Text="{Binding WarrantyPeriod}"/>
                                        <Run Text=" мес."/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="3" Margin="0,10,0,5">
                                    <!-- Старая цена, зачёркнутая, только если есть скидка -->
                                    <TextBlock FontSize="14"
                                               Foreground="Gray"
                                               TextDecorations="Strikethrough"
                                               Visibility="{Binding DiscountedPrice, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Run Text="{Binding Price, StringFormat={}{0:F2}}"/>
                                        <Run Text=" ₽"/>
                                    </TextBlock>

                                    <!-- Текущая цена: если есть скидка → DiscountedPrice, иначе → Price -->
                                    <TextBlock FontSize="22"
                                               FontWeight="Bold"
                                               Foreground="#2E7D32">
                                        <Run Text="{Binding Path=., Converter={StaticResource CurrentPriceConverter}}"/>
                                        <Run Text=" ₽"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <StackPanel Grid.Row="4" Margin="0,5,0,0">
                                    <Button Content="Оформить продажу" 
                                            Command="{Binding DataContext.CreateSaleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Background="#1976D2"
                                            Foreground="White"
                                            FontSize="14"
                                            Padding="10,5"
                                            Margin="0,0,0,5"
                                            Cursor="Hand"/>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Изменить"
                                                Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Background="#FFA000"
                                                Foreground="White"
                                                FontSize="12"
                                                Padding="8,3"
                                                Margin="0,0,5,0"
                                                Cursor="Hand"/>

                                        <Button Content="Удалить"
                                                Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Background="#D32F2F"
                                                Foreground="White"
                                                FontSize="12"
                                                Padding="8,3"
                                                Cursor="Hand"/>
                                    </StackPanel>
                                </StackPanel>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
